export type CanvasOption = { id: string; name: string };

/** Prototype: single canvas; name aligns with canvas details breadcrumb copy. */
export const CANVAS_OPTIONS: CanvasOption[] = [{ id: "main", name: "My new Canvas" }];
